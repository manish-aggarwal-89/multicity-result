# Multi-city integrator run dumps

Dumps from the Sabre-GDS multi-city MCP. Each file under `runs/<test-case-id>/` is one run, named by timestamp.

Written by the GitHub MCP as `manish-aggarwal-89`. Do not gist these to another account.
